Document that branches must be pushed to this repository, not a fork (CONTRIBUTING.md, AGENTS.md, CLAUDE.md), and add a diagnosis-only CI step that annotates fork-head pull requests up front: fork-head runs do not receive the org variable `AXIOM_CORPUS_INGEST_PUBLIC_KEY`, so the authentication-gate tests in `tests/test_state_snap_manual_queue.py` fail by design and previously surfaced only as an opaque coverage-threshold miss (#600 → #606). No test is weakened and no key material is embedded.
